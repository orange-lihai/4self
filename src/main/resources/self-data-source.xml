<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration 
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
	<properties resource="self-db-application.properties">
		<property name="dialect" value="mysql" />
		<property name="mysql.driver" value="com.mysql.jdbc.Driver" />
	</properties>
	<settings>
		<setting name="mapUnderscoreToCamelCase" value="true" />
        <setting name="useGeneratedKeys" value="true"/>
	</settings>
	<environments default="readSelf">
		<environment id="readSelf">
			<transactionManager type="JDBC" />
			<dataSource type="POOLED">
				<property name="driver" value="${mysql.driver}" />
				<property name="url" value="${slave.mysql.url}" />
				<property name="username" value="${slave.mysql.username}" />
				<property name="password" value="${slave.mysql.password}" />
                <property name="poolMaximumActiveConnections" value="100" />
			</dataSource>
		</environment>
        <environment id="writeSelf">
            <transactionManager type="JDBC" />
            <dataSource type="POOLED">
                <property name="driver" value="${mysql.driver}" />
                <property name="url" value="${master.mysql.url}" />
                <property name="username" value="${master.mysql.username}" />
                <property name="password" value="${master.mysql.password}" />
                <property name="poolMaximumActiveConnections" value="100" />
            </dataSource>
        </environment>
	</environments>
	<mappers>
        <mapper resource="mapper/CommonSql.xml" />
	</mappers>
</configuration>
